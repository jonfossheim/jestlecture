{
   "name": "jest",
   "version": "1.0.0",
   "private": true,
   "license": "MIT",
   "scripts": {
      "test": "jest --coverage --watch",
      "test:ci": "jest --ci --reporters='default' --reporters='./github-actions-reporter'"
   },
   "devDependencies": {
      "jest": "^26.6.3"
   }
}
